/target
**/*.rs.bk
*.pdb

# insta のスナップショット差分（承認前）
**/*.snap.new

# ローカル環境
.DS_Store
.env
.claude/settings.local.json
# 読み手の前提知識など、公開面に出さない情報を置く
.claude/*.local.md

# 開発ツールのローカル状態。公開リポジトリに個人の環境設定を含めないため追跡しない。
# Serena はプロジェクトを開いた際に再生成する。
.serena/
