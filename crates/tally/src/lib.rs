//! `tally` — 行指向データの度数集計。
//!
//! 学習用の題材だが、構成は実務の CLI と同じにしてある。
//! 貫かれている不変条件は 2 つ。
//!
//! - **[`core`][] は I/O を持たない。**
//! - **バイナリ（`main.rs`）はロジックを持たない。**
//!   「引数を読む・I/O を開く・結果を出す」だけを担う。
//!
//! この 2 つにより、テストの大半がプロセス起動なしで回る。
//!
//! 各モジュールの説明は下の一覧を参照。**どこに何を置くか、なぜそこなのかは
//! `crates/tally/docs/layout.md` が正本。**

pub mod cli;
pub mod core;
pub mod error;

pub use error::{Result, TallyError};
